When you edit this IRMA schema you have to sign it using the following command:

```shell script
$ irma scheme sign
```

For this you need the `irma` command:

```shell script
$ go install github.com/privacybydesign/irmago/irma
```